machine:
  xcode:
    version: "7.0" # We should try to use the latest Xcode available

dependencies:
  override:
    - "cd Heyzap && pod install"

test:
  override:
    - xcodebuild -showsdks
    - xcrun simctl list
    - ./runUnitTests.sh 9.0
    - ./runUnitTests.sh 8.4